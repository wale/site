---
import icon from "~/assets/icon.svg";
import iconFull from "~/assets/icon_full.svg";

const { class: className, ...props } = Astro.props;
---

<picture>
    <source
        srcset={iconFull.src}
        media="(min-width: 800px)"
    />
    <img class:list={[className]} src={icon.src} alt="Wale's icon" />
</picture>